apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configmap.livechatserver.name }}
data:
  {{ .Values.configmap.livechatserver.key }}: |
    {{ .Values.configmap.livechatserver.value }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.livechatserver.name }}
  labels:
    app: {{ .Values.livechatserver.name }}
    workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.livechatserver.name }}
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: {{ .Values.upgradepolicy.strategy.rollingUpdate.maxSurge }}
      maxUnavailable: {{ .Values.upgradepolicy.strategy.rollingUpdate.maxUnavailable }}
    type: {{ .Values.upgradepolicy.strategy.type }}
  selector:
    matchLabels:
      app: {{ .Values.livechatserver.name }}
      workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.livechatserver.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.livechatserver.name }}
        workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.livechatserver.name }}
    spec:
      containers:
      - name: {{ .Values.livechatserver.name }}
        image: "{{ .Values.livechatserver.image.repository }}:{{ .Values.livechatserver.image.tag }}"
        imagePullPolicy: {{ .Values.livechatserver.image.pullPolicy }}
        env:
        - name: TZ
          value: {{ .Values.environment.general.TZ }}
        volumeMounts:
        - mountPath: {{ .Values.volume.general.volumeMounts.mountPath }}
          name: {{ .Values.volume.general.volumeMounts.name }}
          readOnly: {{ .Values.volume.general.volumeMounts.readOnly }}
        - mountPath: {{ .Values.volume.livechatserver.volumeMounts.mountPath }}
          name: {{ .Values.volume.livechatserver.volumeMounts.name }}
          subPath: {{ .Values.volume.livechatserver.volumeMounts.subPath }}
        securityContext:
          allowPrivilegeEscalation: {{ .Values.spec.container.securityContext.allowPrivilegeEscalation }}
          capabilities: {{ .Values.spec.container.securityContext.capabilities }}
          privileged: {{ .Values.spec.container.securityContext.privileged }}
          readOnlyRootFilesystem: {{ .Values.spec.container.securityContext.readOnlyRootFilesystem }}
          runAsNonRoot: {{ .Values.spec.container.securityContext.runAsNonRoot }}
      hostIPC: {{ .Values.spec.hostIPC }}
      hostPID: {{ .Values.spec.hostPID }}
      volumes:
      - hostPath: 
          path: {{ .Values.volume.general.volumes.hostPath.path }}
          type: {{ .Values.volume.general.volumes.hostPath.type }}
        name: {{ .Values.volume.general.volumes.name }}
      - configMap:
          defaultMode: {{ .Values.volume.livechatserver.volumes.configMap.defaultMode }}
          name: {{ .Values.volume.livechatserver.volumes.configMap.name }}
          optional: {{ .Values.volume.livechatserver.volumes.configMap.optional }}
        name: {{ .Values.volume.livechatserver.volumes.name }}