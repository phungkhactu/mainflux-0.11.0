apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configmap.orimxlivechat.name }}
data:
  {{ .Values.configmap.orimxlivechat.key }}: |
    {{ .Values.configmap.orimxlivechat.value }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.orimxlivechat.name }}
  labels:
    app: {{ .Values.orimxlivechat.name }}
    workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.orimxlivechat.name }}
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: {{ .Values.upgradepolicy.strategy.rollingUpdate.maxSurge }}
      maxUnavailable: {{ .Values.upgradepolicy.strategy.rollingUpdate.maxUnavailable }}
    type: {{ .Values.upgradepolicy.strategy.type }}
  selector:
    matchLabels:
      app: {{ .Values.orimxlivechat.name }}
      workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.orimxlivechat.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.orimxlivechat.name }}
        workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.orimxlivechat.name }}
    spec:
      containers:
      - name: {{ .Values.orimxlivechat.name }}
        image: "{{ .Values.orimxlivechat.image.repository }}:{{ .Values.orimxlivechat.image.tag }}"
        imagePullPolicy: {{ .Values.orimxlivechat.image.pullPolicy }}
        env:
        - name: TZ
          value: {{ .Values.environment.general.TZ }}
        volumeMounts:
        - mountPath: {{ .Values.volume.general.volumeMounts.mountPath }}
          name: {{ .Values.volume.general.volumeMounts.name }}
          readOnly: {{ .Values.volume.general.volumeMounts.readOnly }}
        - mountPath: {{ .Values.volume.orimxlivechat.volumeMounts.mountPath }}
          name: {{ .Values.volume.orimxlivechat.volumeMounts.name }}
          subPath: {{ .Values.volume.orimxlivechat.volumeMounts.subPath }}
        securityContext:
          allowPrivilegeEscalation: {{ .Values.spec.container.securityContext.allowPrivilegeEscalation }}
          capabilities: {{ .Values.spec.container.securityContext.capabilities }}
          privileged: {{ .Values.spec.container.securityContext.privileged }}
          readOnlyRootFilesystem: {{ .Values.spec.container.securityContext.readOnlyRootFilesystem }}
          runAsNonRoot: {{ .Values.spec.container.securityContext.runAsNonRoot }}
      hostIPC: {{ .Values.spec.hostIPC }}
      hostPID: {{ .Values.spec.hostPID }}
      volumes:
      - hostPath: 
          path: {{ .Values.volume.general.volumes.hostPath.path }}
          type: {{ .Values.volume.general.volumes.hostPath.type }}
        name: {{ .Values.volume.general.volumes.name }}
      - configMap:
          defaultMode: {{ .Values.volume.orimxlivechat.volumes.configMap.defaultMode }}
          name: {{ .Values.volume.orimxlivechat.volumes.configMap.name }}
          optional: {{ .Values.volume.orimxlivechat.volumes.configMap.optional }}
        name: {{ .Values.volume.orimxlivechat.volumes.name }}