apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configmap.mobileappstore.name }}
data:
  {{ .Values.configmap.mobileappstore.key }}: |
    {{ .Values.configmap.mobileappstore.value }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.mobileappstore.name }}
  labels:
    app: {{ .Values.mobileappstore.name }}
    workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.mobileappstore.name }}
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: {{ .Values.upgradepolicy.strategy.rollingUpdate.maxSurge }}
      maxUnavailable: {{ .Values.upgradepolicy.strategy.rollingUpdate.maxUnavailable }}
    type: {{ .Values.upgradepolicy.strategy.type }}
  selector:
    matchLabels:
      app: {{ .Values.mobileappstore.name }}
      workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.mobileappstore.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.mobileappstore.name }}
        workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.mobileappstore.name }}
    spec:
      containers:
      - name: {{ .Values.mobileappstore.name }}
        image: "{{ .Values.mobileappstore.image.repository }}:{{ .Values.mobileappstore.image.tag }}"
        imagePullPolicy: {{ .Values.mobileappstore.image.pullPolicy }}
        env:
        - name: TZ
          value: {{ .Values.environment.general.TZ }}
        volumeMounts:
        - mountPath: {{ .Values.volume.general.volumeMounts.mountPath }}
          name: {{ .Values.volume.general.volumeMounts.name }}
          readOnly: {{ .Values.volume.general.volumeMounts.readOnly }}
        - mountPath: {{ .Values.volume.mobileappstore.volumeMounts.mountPath }}
          name: {{ .Values.volume.mobileappstore.volumeMounts.name }}
          subPath: {{ .Values.volume.mobileappstore.volumeMounts.subPath }}
        securityContext:
          allowPrivilegeEscalation: {{ .Values.spec.container.securityContext.allowPrivilegeEscalation }}
          capabilities: {{ .Values.spec.container.securityContext.capabilities }}
          privileged: {{ .Values.spec.container.securityContext.privileged }}
          readOnlyRootFilesystem: {{ .Values.spec.container.securityContext.readOnlyRootFilesystem }}
          runAsNonRoot: {{ .Values.spec.container.securityContext.runAsNonRoot }}
      hostIPC: {{ .Values.spec.hostIPC }}
      hostPID: {{ .Values.spec.hostPID }}
      volumes:
      - hostPath: 
          path: {{ .Values.volume.general.volumes.hostPath.path }}
          type: {{ .Values.volume.general.volumes.hostPath.type }}
        name: {{ .Values.volume.general.volumes.name }}
      - configMap:
          defaultMode: {{ .Values.volume.mobileappstore.volumes.configMap.defaultMode }}
          name: {{ .Values.volume.mobileappstore.volumes.configMap.name }}
          optional: {{ .Values.volume.mobileappstore.volumes.configMap.optional }}
        name: {{ .Values.volume.mobileappstore.volumes.name }}