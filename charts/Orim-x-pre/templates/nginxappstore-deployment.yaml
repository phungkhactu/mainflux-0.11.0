apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configmap.nginxappstore.name }}
data:
  {{ .Values.configmap.nginxappstore.key }}: |
    {{ .Values.configmap.nginxappstore.value }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.nginxappstore.name }}
  labels:
    app: {{ .Values.nginxappstore.name }}
    workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.nginxappstore.name }}
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: {{ .Values.upgradepolicy.strategy.rollingUpdate.maxSurge }}
      maxUnavailable: {{ .Values.upgradepolicy.strategy.rollingUpdate.maxUnavailable }}
    type: {{ .Values.upgradepolicy.strategy.type }}
  selector:
    matchLabels:
      app: {{ .Values.nginxappstore.name }}
      workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.nginxappstore.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.nginxappstore.name }}
        workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.nginxappstore.name }}
    spec:
      containers:
      - name: {{ .Values.nginxappstore.name }}
        image: "{{ .Values.nginxappstore.image.repository }}:{{ .Values.nginxappstore.image.tag }}"
        imagePullPolicy: {{ .Values.nginxappstore.image.pullPolicy }}
        env:
        - name: TZ
          value: {{ .Values.environment.general.TZ }}
        volumeMounts:
        - mountPath: {{ .Values.volume.general.volumeMounts.mountPath }}
          name: {{ .Values.volume.general.volumeMounts.name }}
          readOnly: {{ .Values.volume.general.volumeMounts.readOnly }}
        - mountPath: {{ .Values.volume.nginxappstore.volumeMounts.mountPath }}
          name: {{ .Values.volume.nginxappstore.volumeMounts.name }}
          subPath: {{ .Values.volume.nginxappstore.volumeMounts.subPath }}
        securityContext:
          allowPrivilegeEscalation: {{ .Values.spec.container.securityContext.allowPrivilegeEscalation }}
          capabilities: {{ .Values.spec.container.securityContext.capabilities }}
          privileged: {{ .Values.spec.container.securityContext.privileged }}
          readOnlyRootFilesystem: {{ .Values.spec.container.securityContext.readOnlyRootFilesystem }}
          runAsNonRoot: {{ .Values.spec.container.securityContext.runAsNonRoot }}
      hostIPC: {{ .Values.spec.hostIPC }}
      hostPID: {{ .Values.spec.hostPID }}
      volumes:
      - hostPath: 
          path: {{ .Values.volume.general.volumes.hostPath.path }}
          type: {{ .Values.volume.general.volumes.hostPath.type }}
        name: {{ .Values.volume.general.volumes.name }}
      - configMap:
          defaultMode: {{ .Values.volume.nginxappstore.volumes.configMap.defaultMode }}
          name: {{ .Values.volume.nginxappstore.volumes.configMap.name }}
          optional: {{ .Values.volume.nginxappstore.volumes.configMap.optional }}
        name: {{ .Values.volume.nginxappstore.volumes.name }}