apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configmap.sohoafrontend.name }}
data:
  {{ .Values.configmap.sohoafrontend.key }}: |
    {{ .Values.configmap.sohoafrontend.value }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.sohoafrontend.name }}
  labels:
    app: {{ .Values.sohoafrontend.name }}
    workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.sohoafrontend.name }}
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: {{ .Values.upgradepolicy.strategy.rollingUpdate.maxSurge }}
      maxUnavailable: {{ .Values.upgradepolicy.strategy.rollingUpdate.maxUnavailable }}
    type: {{ .Values.upgradepolicy.strategy.type }}
  selector:
    matchLabels:
      app: {{ .Values.sohoafrontend.name }}
      workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.sohoafrontend.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.sohoafrontend.name }}
        workload.user.cattle.io/workloadselector: deployment-{{.Release.Namespace}}-{{ .Values.sohoafrontend.name }}
    spec:
      containers:
      - name: {{ .Values.sohoafrontend.name }}
        image: "{{ .Values.sohoafrontend.image.repository }}:{{ .Values.sohoafrontend.image.tag }}"
        imagePullPolicy: {{ .Values.sohoafrontend.image.pullPolicy }}
        env:
        - name: TZ
          value: {{ .Values.environment.general.TZ }}
        volumeMounts:
        - mountPath: {{ .Values.volume.general.volumeMounts.mountPath }}
          name: {{ .Values.volume.general.volumeMounts.name }}
          readOnly: {{ .Values.volume.general.volumeMounts.readOnly }}
        - mountPath: {{ .Values.volume.sohoafrontend.volumeMounts.mountPath }}
          name: {{ .Values.volume.sohoafrontend.volumeMounts.name }}
          subPath: {{ .Values.volume.sohoafrontend.volumeMounts.subPath }}
        securityContext:
          allowPrivilegeEscalation: {{ .Values.spec.container.securityContext.allowPrivilegeEscalation }}
          capabilities: {{ .Values.spec.container.securityContext.capabilities }}
          privileged: {{ .Values.spec.container.securityContext.privileged }}
          readOnlyRootFilesystem: {{ .Values.spec.container.securityContext.readOnlyRootFilesystem }}
          runAsNonRoot: {{ .Values.spec.container.securityContext.runAsNonRoot }}
      hostIPC: {{ .Values.spec.hostIPC }}
      hostPID: {{ .Values.spec.hostPID }}
      volumes:
      - hostPath: 
          path: {{ .Values.volume.general.volumes.hostPath.path }}
          type: {{ .Values.volume.general.volumes.hostPath.type }}
        name: {{ .Values.volume.general.volumes.name }}
      - configMap:
          defaultMode: {{ .Values.volume.sohoafrontend.volumes.configMap.defaultMode }}
          name: {{ .Values.volume.sohoafrontend.volumes.configMap.name }}
          optional: {{ .Values.volume.sohoafrontend.volumes.configMap.optional }}
        name: {{ .Values.volume.sohoafrontend.volumes.name }}